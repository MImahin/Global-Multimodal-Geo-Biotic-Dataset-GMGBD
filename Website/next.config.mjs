/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export', 
  basePath: '/Global-Multimodal-Geo-Biotic-Dataset-GMGBD',
  images: {
    unoptimized: true, 
  },
};

export default nextConfig;